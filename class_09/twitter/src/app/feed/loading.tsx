import LoadingSpinner from '../../components/loading-spinner';

// Loading component for the feed page, loading is a special Next.js component that is used to show a loading spinner

export default function Loading() {
	return <LoadingSpinner />;
}
